name: CI

# `pull_request` roda sobre o merge commit — o código como vai ficar em `main`,
# não como está na branch. O `push` fica restrito a `main` para não duplicar cada
# run da branch: sem isso, o mesmo commit dispara os dois eventos.
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Em `main` cada commit é uma promoção e precisa do próprio resultado; cancelar
  # deixaria um merge sem CI se dois caíssem seguidos.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # Hook é local: `--no-verify` burla, e commit feito pela UI do GitHub nunca
  # roda hook nenhum. Como o assunto do commit é o que decide a versão que o
  # release-please publica, o portão precisa existir também aqui.
  #
  # Merge commits são ignorados pelo commitlint por padrão — conferido contra os
  # 14 commits de `main`: os 9 de trabalho passam, os 5 de merge são ignorados.
  commits:
    name: commitlint
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      # `fetch-depth: 0` porque validar um intervalo exige os dois extremos, e
      # o checkout raso do PR traz só o merge commit.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: yarn

      # Este job só precisa do CLI do commitlint. Sem a flag, o install dispara
      # `nuxt prepare` e `husky`, que aqui não servem para nada.
      - run: yarn install --frozen-lockfile --ignore-scripts

      # Os SHAs vão por `env` em vez de interpolados no `run`. Estes dois campos
      # são hex de 40 caracteres gerados pelo GitHub e não dão para injetar —
      # mas `${{ }}` dentro de `run` é o padrão que vira injeção no dia em que
      # alguém copiar a linha para um campo que o autor do PR controla, como
      # `.title` ou `.head.ref`.
      - name: valida os assuntos dos commits do PR
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          yarn commitlint \
            --from "$BASE_SHA" \
            --to "$HEAD_SHA" \
            --verbose

  checks:
    name: lint · typecheck · test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: yarn

      - run: yarn install --frozen-lockfile

      - run: yarn lint

      - run: yarn typecheck

      - run: yarn test

  # Sem banco — os fluxos E2E rodam deslogados, que é um modo legítimo, já que
  # jogar não exige conta.
  e2e:
    name: e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: yarn

      - run: yarn install --frozen-lockfile

      - run: yarn playwright install --with-deps chromium

      - run: yarn build

      - run: yarn test:e2e

      # `trace: 'on-first-retry'` grava em test-results/, fora do relatório —
      # sem as duas pastas, uma falha no CI não deixa nem trace nem report.
      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
