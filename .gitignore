# dependências
node_modules/

# build
.nuxt/
.output/
dist/
.vercel/

# segredos — entram antes de existir qualquer segredo para vazar
.env
.env.*
!.env.example

# cache do crawl da PokeAPI (a saída em public/data/ é commitada)
.cache/pokeapi/

# artefatos de typecheck
*.tsbuildinfo

# testes
coverage/
test-results/
playwright-report/

# ferramentas locais — só o global do autor ignorava isto, então num clone de
# outra máquina apareceria como untracked
.claude/settings.local.json
.code-review-graph/

# sistema
.DS_Store
*.log
